services:
  - type: web
    name: ponnaloy-web
    runtime: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile; pnpm --filter @ponnaloy/web build
    startCommand: cd apps/web && pnpm start
    envVars:
      - key: NODE_ENV
        value: production
